#include "vizgraphwidget.h"

/*
vizGraphWidget::vizGraphWidget(QWidget *parent) : QWidget(parent) {
  setObjectName(QStringLiteral("tab"));
  canvas = new QGraphicsView(this);
  canvas->setObjectName(QStringLiteral("vizCanvas"));
  canvas->setGeometry(QRect(0, 0, 960, 770));
}

//tabWidget->addTab(tab, QString());
*/
